apiVersion: v1
kind: Service
metadata:
  name: client
  namespace: project-mgmt
  labels:
    app: client
spec:
  selector:
    app: client
  ports:
    - port: 80
      targetPort: 80
  type: ClusterIP
