apiVersion: v1
kind: Service
metadata:
  name: server
  namespace: project-mgmt
  labels:
    app: server
spec:
  selector:
    app: server
  ports:
    - port: 5001
      targetPort: 5001
  type: ClusterIP
